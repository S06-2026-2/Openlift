// Sprint 2 — Cliente HTTP (Dio) para falar com o backend FastAPI.
//
// Centraliza base URL, interceptor de JWT (anexa `Authorization: Bearer`)
// e tratamento padronizado de erros vindos da API.

class ApiClient {
  // TODO(Sprint 2): configurar Dio com baseUrl (via AppConstants).
  // TODO(Sprint 2): interceptor que injeta o access token salvo no SecureStorageService.
  // TODO(Sprint 4): interceptor de refresh token em respostas 401.
}
