// Sprint 1 — Constantes globais do app.

class AppConstants {
  // TODO(Sprint 1): apiBaseUrl (dev/staging/prod via --dart-define).
  // TODO(Sprint 3): defaultNostrRelays (ex.: relay.damus.io, nos.lol, relay.nostr.band).
}
