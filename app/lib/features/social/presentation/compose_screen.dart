// Sprint 4 — Tela de composição: compartilhar um treino (kind 1) ou escrever
// um artigo (NIP-23, kind 30023). Assinatura local via NostrIdentityService.

class ComposeScreen {
  // TODO(Sprint 4): modo "treino" (resumo pré-preenchido a partir de um WorkoutModel).
  // TODO(Sprint 4): modo "artigo" (título + corpo em markdown).
  // TODO(Sprint 4): feedback de publicação por relay (sucesso/erro).
}
