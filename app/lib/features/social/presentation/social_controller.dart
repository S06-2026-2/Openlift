// Sprint 3/4 — Controller (Riverpod) do feed e da composição/publicação.

class SocialController {
  // TODO(Sprint 3): estado do feed (Stream de NostrEventModel via NostrRelayService).
  // TODO(Sprint 4): shareWorkout(WorkoutModel) -> assina (NostrIdentityService) + publica (NostrRelayService)
  // TODO(Sprint 4): shareArticle(title, body) -> kind 30023 (NIP-23)
  // TODO(Sprint 4): toggleLike(eventId) -> kind 7 (NIP-25)
}
