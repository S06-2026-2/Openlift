// Sprint 2 — Modelo de um treino (data, lista de séries, referência ao usuário).

class WorkoutModel {
  // TODO(Sprint 2): campos id, date, sets (List<SetModel>).
  // TODO(Sprint 4): campo sharedEventId (preenchido quando o treino é publicado no NOSTR).
}
