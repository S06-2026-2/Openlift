// Sprint 2 — Chamadas de autenticação à API (register, login, refresh)
// e registro do npub gerado localmente.

class AuthRepository {
  // TODO(Sprint 2): register(email, password) -> UserModel
  // TODO(Sprint 2): login(email, password) -> UserModel + tokens
  // TODO(Sprint 2): registerNostrIdentity(npub) -> POST /me/nostr-identity
  // TODO(Sprint 4): refreshToken()
}
