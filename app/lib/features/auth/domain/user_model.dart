// Sprint 2 — Modelo de usuário no app.
//
// Espelha o retorno de /auth/login e /auth/register no backend:
// { id, email, npub, createdAt }.

class UserModel {
  // TODO(Sprint 2): campos id, email, npub, createdAt.
  // TODO(Sprint 2): fromJson / toJson.
}
