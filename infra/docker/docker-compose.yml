# Orquestração local: API + banco de dados (Sprint 1), Redis + worker (Sprint 4).
#
# TODO(Sprint 1): service `db`      — postgres:16, volume nomeado, healthcheck
# TODO(Sprint 1): service `adminer` — inspeção visual do Postgres em dev
# TODO(Sprint 1): service `api`     — build: ../../api, Dockerfile.api, depende de `db`
# TODO(Sprint 4): service `redis`   — cache e fila do worker de sincronização NOSTR
# TODO(Sprint 4): service `worker`  — mesma imagem da API, comando roda nostr_sync_worker
#
# services:
#   db: ...
#   adminer: ...
#   api: ...
#   redis: ...
#   worker: ...
