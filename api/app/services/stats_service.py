"""Regras de negócio de agregação: volume por período, por grupo muscular e
a matriz do heatmap (Sprint 5, com cache Redis para as consultas pesadas)."""

# TODO(Sprint 3): compute_volume_stats(db, user, group_by) -> VolumeStatsOut
# TODO(Sprint 3): compute_muscle_group_volume(db, user) -> list[MuscleGroupVolumeOut]
# TODO(Sprint 5): compute_heatmap(db, user, date_from, date_to) -> list[HeatmapCellOut]
