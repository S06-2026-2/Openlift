"""Model `users` — conta interna do OpenLift (e-mail + senha com hash).

Relaciona 1:1 com NostrIdentity e 1:N com Workout.
"""

# TODO(Sprint 1): class User(Base) — id, email, hashed_password, created_at
