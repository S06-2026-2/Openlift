"""Schemas Pydantic v2 de treino, série e exercício."""

# TODO(Sprint 2): class SetIn / SetOut — exercise_id, reps, weight_kg, rpe
# TODO(Sprint 2): class WorkoutIn(BaseModel) — date, sets: list[SetIn]
# TODO(Sprint 2): class WorkoutOut(BaseModel) — id, date, sets: list[SetOut], shared_event_id
# TODO(Sprint 3): class WorkoutFilters — date_from, date_to, exercise_id, muscle_group_id
