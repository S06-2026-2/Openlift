"""Schemas Pydantic v2 de autenticação e registro de identidade NOSTR."""

# TODO(Sprint 2): class RegisterRequest(BaseModel) — email, password
# TODO(Sprint 2): class LoginRequest(BaseModel) — email, password
# TODO(Sprint 2): class TokenPair(BaseModel) — access_token, refresh_token, token_type
# TODO(Sprint 4): class RefreshRequest(BaseModel) — refresh_token
# TODO(Sprint 2): class NostrIdentityIn(BaseModel) — npub
