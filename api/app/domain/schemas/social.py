"""Schemas Pydantic v2 de compartilhamento, curtidas e ranking."""

# TODO(Sprint 4): class SharedEventIn — workout_id (opcional), event_id, kind
# TODO(Sprint 4): class LikeCountOut — event_id, count, liked_by_me
# TODO(Sprint 4): class RankingEntryOut — shared_event_id, score
