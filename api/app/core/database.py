"""Engine e sessão do SQLAlchemy 2.0 (Postgres via app.infra.db.session).

Sprint 1: engine síncrono/assíncrono + Base declarativa para os models.
"""

# TODO(Sprint 1): engine = create_engine(settings.DATABASE_URL)
# TODO(Sprint 1): class Base(DeclarativeBase) — herdada por todos os models em app/domain/models.
# TODO(Sprint 1): get_db() -> Generator[Session] (dependency do FastAPI)
